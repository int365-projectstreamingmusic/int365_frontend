import { createStore } from "vuex";
import authentication from "./authentication";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    authentication,
  },
});
